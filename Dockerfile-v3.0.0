# Use Tomcat 9 on JDK 17 (Corretto, Amazon Linux 2)
FROM tomcat:9.0.111-jdk17-corretto-al2

# Remove default apps (ROOT, docs, examples, etc.) to keep image lean
RUN rm -rf /usr/local/tomcat/webapps/*

# Copy your WAR and deploy it as ROOT so it serves at "/"
COPY Signup-Login-v3.0.0.war /usr/local/tomcat/webapps/ROOT.war

# (Optional) If you want to reduce JVM startup entropy blocking
# ENV CATALINA_OPTS="$CATALINA_OPTS -Djava.security.egd=file:/dev/./urandom"